This is the dab_step-write-a-block package meant as a guide to building
out-of-tree packages. To use the dab_step blocks, the Python namespaces
is in 'dab_step', which is imported as:

    import dab_step

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(dab_step)
